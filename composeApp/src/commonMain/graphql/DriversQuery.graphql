query DriversQuery($year: Int, $round: Int, $constructorId: String) {
    drivers(year: $year, round: $round, constructorId: $constructorId) {
        id
        givenName
        familyName
        nationality
        number
    }
}
