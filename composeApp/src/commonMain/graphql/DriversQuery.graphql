query DriversQuery($year: Int, $round: Int) {
    drivers(year: $year, round: $round) {
        code
        dateOfBirth
        familyName
        givenName
        id
        nationality
        number
        url
    }
}
