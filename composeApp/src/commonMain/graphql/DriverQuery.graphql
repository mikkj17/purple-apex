query DriverQuery($driverId: String!) {
    driver(driverId: $driverId) {
        code
        dateOfBirth
        familyName
        givenName
        id
        nationality
        number
        url
    }
}
